import {{name}} from './index';
{{#if actions}}import deps from 'dependencies';{{/if}}

export default {
    path: {{{route}}},
    exact: true,
    component: {{name}},
    {{#if actions}}
    load: (params, search) => deps().actions.{{name}}.load({ params, search }),
    {{/if}}
};
