/**
 * Generated by createManifest.js
 * DO NOT directly edit this file !!!!!!
 */

module.exports = {
    get: () => {
        return {
        {{#each types}}
            '{{name}}': {
                {{#each domains}}
                    '{{domain}}': require('{{file}}').default,
                {{/each}}
            },
        {{/each}}
        };
    },
    contexts: {
        {{#each contexts}}
        '{{context}}': typeof window !== 'undefined' && require.context('{{modulePath}}', true, /{{filePattern}}/),
        {{/each}}
    },
    listContexts: () => {
        return {{{contextObj}}}
    },
    list: () => {
        return {{{manifest}}};
    },
}
